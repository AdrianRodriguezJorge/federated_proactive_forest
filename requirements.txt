# Core dependencies (must match pyproject.toml)
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
pyyaml>=6.0
pydantic>=2.0

# UI (optional: pip install -e ".[ui]")
streamlit>=1.35
plotly>=5.20

# Dev (optional: pip install -e ".[dev]")
pytest>=7.4
pytest-cov>=4.1

# Optimization (optional: pip install -e ".[opt]")
optuna>=3.5
joblib>=1.3
tensorly>=0.8

# FLEX Framework (for full FL orchestration)
flexible-fl
flex
flextrees
setuptools<70
