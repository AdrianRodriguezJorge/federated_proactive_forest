datasets:
  - type: "Iris"

strategies:
  - "s1_simple_pool"
  - "pw"

federation:
  n_clients: 3
  distribution: "iid" 

model:
  n_estimators: 10 
  alpha: 0.1
  use_progressive_stopping: true
  convergence: 0.002
  episode_size: 5

output_file: "results/test_benchmark.csv"
