* Crear pruebas unitarias usando `pytest` para el archivo indicado.
* Las pruebas deben validar casos extremos (ej. datos vacíos, inputs no numéricos, desbalanceo extremo de clases).
* Asegurar que el archivo de pruebas se guarde en el directorio `tests/` con el prefijo `test_`.
