* Ejecutar las pruebas unitarias usando: `cmd.exe /c "pytest tests/"` para asegurar que nada esté roto.
* Ejecutar `cmd.exe /c "git status"` para analizar los archivos modificados.
* Generar una propuesta de mensaje de commit estructurado usando el estándar "Conventional Commits" (ej. `feat(metrics): add macro average to evaluation pipeline`).
* Esperar la confirmación del usuario para proceder a realizar el commit de manera segura.
